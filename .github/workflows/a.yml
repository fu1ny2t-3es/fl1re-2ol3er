on:
  workflow_dispatch:
  schedule:
    - cron: '45 14 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/fl1re-2ol3er@cron
        with:
          token: ${{ secrets.token }}
